describe('description', () => {
	beforeEach(() => {

	});

	beforeEach(() => {
	});

	it("12", () => {
		console.log('valami');

		expect(1).toBe(1);
		expect(() => {
		}).toThrow();

		const a = 1;

		expect(1).toBe(1);
		expect(() => {
		}).toThrow();
	});
});
